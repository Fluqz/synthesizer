// import { get, set } from 'idb-keyval';

export class DB {

    static name: string

    private static storeSuffix: string = '-store'
    private static dbSuffix: string = '-db'


    static set(key: string, data: any) {

        // return set(key, data)
    }

    static get(key: string) {

        // return get(key)
    }
}